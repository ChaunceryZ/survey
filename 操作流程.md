# Smart Survey 操作流程

本文档面向 Smart Survey 的实际使用者，说明如何完成一份问卷从创建、编辑、发布、填写、回收分析到导出数据的完整流程。

## 1. 启动项目

首次使用前安装依赖：

```bash
npm install
```

启动开发环境：

```bash
npm run dev
```

浏览器打开终端中显示的本地地址，通常是：

```text
http://localhost:5173
```

## 2. 创建问卷

1. 进入首页。
2. 点击右上角「创建问卷」。
3. 进入问卷编辑器。
4. 填写问卷标题。
5. 根据需要填写问卷描述。

注意：

- 问卷标题必填。
- 问卷至少需要添加一个问题后才能发布。
- 点击「保存草稿」会保存为草稿状态，草稿不能被填写。

## 3. 添加题目

在编辑器中点击「添加问题」，选择题型。

当前支持：

| 题型 | 使用场景 |
| --- | --- |
| 单选题 | 用户只能选择一个答案 |
| 多选题 | 用户可以选择多个答案 |
| 填空题 | 用户输入简短文本 |
| 多行文本 | 用户输入较长反馈 |
| 评分题 | 用户按分值进行评价 |
| 矩阵题 | 多个维度使用同一组选项评价 |
| 排序题 | 用户对选项进行优先级排序 |

添加题目后，可以点击题目卡片右侧的编辑按钮进行配置。

## 4. 配置题目

### 4.1 基础配置

每道题都可以配置：

- 问题标题
- 问题描述
- 是否必答

选择题和排序题可以配置选项。

评分题可以配置：

- 最小值
- 最大值
- 最低分标签
- 最高分标签

矩阵题可以配置：

- 矩阵行
- 矩阵列

### 4.2 自定义校验

文本题和评分题可以开启「自定义校验」。

支持规则：

| 校验类型 | 填写方式 | 示例 |
| --- | --- | --- |
| 最小长度 | 输入最小字符数 | `6` |
| 最大长度 | 输入最大字符数 | `100` |
| 正则表达式 | 输入正则规则 | `^1[3-9]\d{9}$` |
| 数值范围 | 输入最小值和最大值 | `1,5` |

建议同时填写清晰的错误提示，例如：

```text
请输入 6 位以上内容
```

### 4.3 条件逻辑

当问卷中已有其他题目时，可以为题目配置「条件逻辑」。

支持条件：

| 条件 | 说明 |
| --- | --- |
| 等于 | 上一道题答案等于指定值 |
| 不等于 | 上一道题答案不等于指定值 |
| 包含 | 多选答案包含指定值 |
| 大于 | 数值答案大于指定值 |
| 小于 | 数值答案小于指定值 |

支持动作：

| 动作 | 效果 |
| --- | --- |
| 显示本题 | 条件满足时显示当前题 |
| 隐藏本题 | 条件满足时隐藏当前题 |
| 跳转到题目 | 条件满足时跳转到指定题目 |

示例：

1. Q1 询问“是否使用过本产品”。
2. Q2 配置条件逻辑：当 Q1 等于“是”时显示。
3. 用户选择“否”时，Q2 不显示。

## 5. 调整题目顺序

在编辑器的问题列表中，可以拖拽题目卡片调整顺序。

建议先完成题目内容，再统一调整顺序；如果已经配置条件逻辑，调整顺序后需要重新检查跳转目标是否符合预期。

## 6. 配置问卷设置

编辑器右侧提供问卷设置。

| 设置 | 说明 |
| --- | --- |
| 允许保存进度 | 用户填写时自动保存未提交答案 |
| 显示进度条 | 填写页顶部显示完成进度 |
| 随机排列题目 | 每次填写时随机生成题目顺序 |
| 随机排列选项 | 每次填写时随机生成选项顺序 |
| 关闭时间 | 超过该时间后问卷不能继续填写 |

说明：

- 随机题目和随机选项会在用户进入填写页时生成一次，本次填写过程中保持稳定。
- 关闭时间只影响填写和提交，不影响编辑问卷。

## 7. 保存草稿

点击「保存草稿」后，问卷会回到首页并显示为「草稿」状态。

草稿状态下：

- 可以继续编辑。
- 不能填写。
- 不会接受提交。

适合在问卷尚未准备好时临时保存。

## 8. 发布问卷

发布前请确认：

1. 问卷标题已填写。
2. 至少添加了一道题。
3. 必答题、选项、评分范围、矩阵行列配置正确。
4. 条件逻辑和跳转目标符合预期。
5. 关闭时间符合预期。

点击「发布问卷」后，问卷会变为「已发布」状态。

已发布状态下：

- 首页会显示填写入口。
- 可以进入填写页提交回答。
- 如果设置了关闭时间，过期后不能继续提交。

## 9. 填写问卷

在首页找到已发布问卷，点击填写按钮进入填写页。

填写流程：

1. 按当前题目逐题作答。
2. 点击「下一题」进入下一题。
3. 点击「上一题」返回上一题。
4. 点击底部题目导航点可以跳转到指定题目。
5. 最后一题点击「提交问卷」。
6. 提交成功后进入感谢页。

校验规则：

- 必答题不能为空。
- 多选题至少选择一个选项。
- 矩阵必答题需要完成所有行。
- 自定义校验不通过时会显示错误提示。
- 提交时会检查所有可见题目，避免跳过必答题。

保存进度：

- 如果问卷开启「允许保存进度」，未提交答案会自动保存到本地浏览器。
- 刷新页面后可以继续填写。
- 提交成功后，本地进度会自动清理。

## 10. 查看感谢页

提交成功后会进入感谢页。

用户可以：

- 确认提交成功。
- 返回首页。

## 11. 查看数据分析

在首页点击问卷的分析按钮进入分析页。

分析页展示：

| 指标 | 说明 |
| --- | --- |
| 回收数量 | 当前问卷收到的提交数量 |
| 完成率 | 必答题完成情况 |
| 题目数量 | 当前问卷题目总数 |

不同题型的分析方式：

| 题型 | 分析内容 |
| --- | --- |
| 单选题 | 各选项占比图 |
| 多选题 | 各选项选择次数和占比 |
| 评分题 | 平均分和分布柱状图 |
| 矩阵题 | 每行各选项选择分布 |
| 排序题 | 每个选项的平均名次 |
| 文本题 | 展示部分文本回答 |

如果当前问卷还没有回答数据，分析页会显示暂无数据，导出按钮不可用。

## 12. 导出数据

分析页支持两种导出格式。

### 12.1 导出 JSON

JSON 文件包含：

- 问卷结构
- 回答数据
- 导出时间

适合备份或继续做程序化处理。

### 12.2 导出 CSV

CSV 文件按表格形式组织回答数据，适合在 Excel、Numbers、WPS 或其他数据分析工具中打开。

导出前建议确认：

- 问卷已有至少一条回答。
- 文本题内容不含异常格式。
- 矩阵题和排序题结果在分析页展示正常。

## 13. 复制和删除问卷

### 13.1 复制问卷

在首页点击复制按钮，可以创建一份草稿副本。

复制后的问卷：

- 标题会追加“副本”。
- 状态为草稿。
- 不会继承原问卷的回答数据。
- 修改副本不会影响原问卷。

### 13.2 删除问卷

在首页点击删除按钮，会出现确认提示。

确认后问卷会被删除。

注意：

- 删除操作不可恢复。
- 删除问卷前建议先导出重要数据。

## 14. 常见问题

### 为什么草稿问卷不能填写？

草稿用于编辑和准备阶段。只有发布后的问卷才允许填写和提交。

### 为什么已发布问卷无法填写？

可能原因：

- 问卷已被关闭。
- 问卷设置了关闭时间，且当前时间已超过关闭时间。
- 当前浏览器本地数据被清理，问卷不存在。

### 为什么刷新后答案还在？

问卷开启了「允许保存进度」。系统会把未提交答案保存在当前浏览器的 `localStorage` 中。

### 为什么换浏览器后看不到问卷？

当前项目是本地存储版本，数据只保存在当前浏览器中，不会跨浏览器或跨设备同步。

### 为什么分析页没有数据？

需要至少提交一份回答后，分析页才会展示统计结果。

## 15. 推荐使用流程

建议按以下顺序使用：

1. 创建问卷。
2. 添加并配置所有题目。
3. 配置问卷设置。
4. 保存草稿。
5. 重新进入编辑页检查内容。
6. 发布问卷。
7. 进入填写页试填一次。
8. 查看分析页确认数据正常。
9. 正式收集回答。
10. 导出 JSON 或 CSV。

