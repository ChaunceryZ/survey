# Smart Survey 后续改进计划

## 目标

把当前单机版问卷系统从“功能可演示”推进到“行为可靠、体验完整、便于继续扩展”的状态。优先处理会造成数据污染、误跳转、错误提交的问题，再补齐 README 中已经承诺但尚未完整实现的功能。

## 当前项目判断

- 技术栈清晰：Vue 3、TypeScript、Vite、Pinia、Vue Router、SCSS。
- 功能闭环已存在：创建问卷、发布问卷、填写问卷、提交回答、查看分析、导出数据。
- 数据存储完全依赖 localStorage，适合本地演示和轻量使用，暂不具备多人协作、权限控制、真实发布分发能力。
- 构建可以通过，但存在 Sass `darken()` 废弃警告和分析页 ECharts chunk 偏大的提示。
- README 中部分能力描述超前于当前实现，例如跳转逻辑、完整验证规则、保存进度、随机选项、关闭日期等。

## 第一阶段：修复高风险行为

### 1. 修复首页按钮冒泡

问题：问卷卡片整体点击会进入编辑页，但“填写问卷”和“数据分析”按钮没有阻止冒泡，点击按钮时可能同时触发编辑跳转。

处理：
- 为填写、分析按钮事件增加 `event.stopPropagation()`。
- 统一首页操作按钮的事件处理方式，避免后续新增按钮时重复踩坑。

验收：
- 点击填写按钮只进入填写页。
- 点击分析按钮只进入分析页。
- 点击卡片空白区域仍进入编辑页。

### 2. 修复编辑页浅拷贝导致的未保存污染

问题：编辑已有问卷时只做了浅拷贝，`questions`、`settings` 等嵌套数据仍可能引用 store 中的原对象。用户未保存就修改题目时，内存状态可能已经被改动。

处理：
- 编辑页加载问卷时做深拷贝。
- 保存时再提交完整副本到 store。
- 取消、返回时不影响原问卷。

验收：
- 编辑已有问卷后新增/删除/修改题目，直接返回首页，原问卷保持不变。
- 保存后修改才生效。

### 3. 修复复制问卷共享引用

问题：复制问卷时使用浅复制，副本可能与原问卷共享问题对象或配置对象。

处理：
- 复制问卷时深拷贝问题、选项、矩阵、评分、设置等嵌套结构。
- 复制品保持草稿状态，并生成新的问卷 ID。
- 可选：为副本内的问题和选项也重新生成 ID，避免逻辑引用和分析数据混淆。

验收：
- 修改副本的问题不会影响原问卷。
- 原问卷回答数据不会混入副本。

### 4. 增加填写状态校验

问题：当前只要知道 `/fill/:id`，草稿或已关闭问卷也可能被填写提交。

处理：
- 填写页只允许 `published` 状态问卷进入。
- `submitResponse` 在 store 层也校验问卷状态。
- 对草稿、已关闭、不存在问卷展示明确提示并引导返回首页。

验收：
- 草稿问卷不能填写。
- 已关闭问卷不能提交。
- store 层直接调用提交接口也不能绕过状态限制。

## 第二阶段：修正答题过程可靠性

### 1. 固定随机题目顺序

问题：随机题目顺序在 computed 中计算，答案变化或响应式更新可能导致题目顺序重新洗牌。

处理：
- 进入填写页时生成一次题目顺序，并保存在本次填写会话中。
- 后续只根据逻辑条件过滤显示，不重新随机基础顺序。

验收：
- 开始填写后，题目顺序在本次提交前保持稳定。
- 切换答案不会让已看到的问题位置突然变化。

### 2. 实现随机选项

问题：设置中有 `randomizeOptions`，但表单组件实际没有使用。

处理：
- 进入填写页时为每道选择题生成一次选项顺序。
- 单选、多选、排序题使用稳定后的选项顺序渲染。

验收：
- 开启随机选项后，填写时选项顺序随机但本次会话内稳定。
- 关闭随机选项后，按编辑顺序展示。

### 3. 完善排序题默认提交

问题：排序题默认顺序只存在组件内部，用户不拖拽时可能没有向 answers 写入答案。

处理：
- 排序题初始化时向父组件提交默认顺序，或提交时将默认顺序视为有效答案。
- 必答校验覆盖排序题。

验收：
- 排序题不拖拽也能提交默认排序。
- 必答排序题不会因为未触发拖拽而误判为空。

### 4. 修正进度计算

问题：当前进度基于当前题号除以总题数，最后一题提交前显示可能不是 100%。

处理：
- 使用已完成题数或当前位置加一计算展示进度。
- 提交成功前最后一步可显示 100%。

验收：
- 第一题显示合理起始进度。
- 最后一题显示 100% 或接近用户预期的完成状态。

## 第三阶段：补齐 README 已声明能力

### 1. 完善逻辑规则

当前类型中存在：
- `equals`
- `not_equals`
- `contains`
- `greater_than`
- `less_than`

当前动作中存在：
- `show`
- `hide`
- `jump`

处理：
- 补齐 `greater_than` 和 `less_than` 判断。
- 实现 `jump` 跳转动作，明确跳转优先级。
- 在编辑器中增加逻辑配置 UI，避免只能通过数据手工写入。

验收：
- 条件显示、隐藏、跳转均可在编辑器中配置。
- 填写流程符合配置结果。

### 2. 完善验证规则

当前类型中存在：
- `required`
- `min_length`
- `max_length`
- `pattern`
- `range`

处理：
- 补齐 `pattern` 和 `range` 校验。
- 在编辑器中增加验证规则配置 UI。
- 校验错误信息可自定义。

验收：
- 文本题支持长度和正则校验。
- 评分或数字输入支持范围校验。
- 用户看到明确错误提示。

### 3. 实现保存进度

问题：设置中有 `allowSave`，但当前没有实际保存答题进度。

处理：
- 使用 localStorage 保存未提交回答。
- 按问卷 ID 区分草稿答案。
- 提交成功后清理对应进度。

验收：
- 允许保存进度时，刷新页面后能恢复已填答案。
- 禁用保存进度时，不持久化未提交答案。

### 4. 支持关闭日期

问题：类型中有 `closeDate`，但编辑器和填写页没有使用。

处理：
- 编辑器增加关闭日期设置。
- 填写页根据关闭日期阻止过期问卷提交。
- 首页状态或提示中体现关闭状态。

验收：
- 超过关闭日期后不能填写。
- 未到关闭日期的已发布问卷仍可填写。

## 第四阶段：完善预览和分析体验

### 1. 补齐预览题型

问题：预览页目前主要覆盖单选、多选、输入、文本、评分，矩阵题和排序题展示不完整。

处理：
- 为矩阵题增加表格预览。
- 为排序题增加排序列表预览。
- 预览页可复用真实表单组件的只读模式，减少重复实现。

验收：
- 所有题型在预览页都有合理展示。
- 预览效果与填写页一致或接近。

### 2. 改进分析页统计

处理：
- 文本题支持查看更多回答。
- 排序题支持名次统计。
- 矩阵题支持每行各选项分布，而不是只显示最高项。
- 空数据时禁用导出按钮或给出提示。

验收：
- 每种题型都有对应分析方式。
- 导出 JSON/CSV 内容与页面统计一致。

### 3. 降低分析页包体积

问题：ECharts chunk 超过 500KB。

处理：
- 保持分析页懒加载。
- 检查 ECharts 按需导入是否还能进一步精简。
- 必要时把图表初始化逻辑拆成独立模块。

验收：
- 构建警告减少或可解释。
- 首页、填写页不承担分析页图表成本。

## 第五阶段：工程质量整理

### 1. 添加基础测试

建议优先覆盖：
- store 创建、复制、发布、关闭、提交逻辑。
- 填写页必答校验。
- 编辑页保存与取消行为。
- CSV/JSON 导出格式函数。

验收：
- 关键业务逻辑有自动化测试。
- 修复浅拷贝、状态校验等问题时有回归保障。

### 2. 整理构建产物和忽略规则

问题：仓库中存在 `dist`、`*.tsbuildinfo`，当前还有未跟踪的 `.idea/`。

处理：
- 明确是否需要提交 `dist`。
- 若不需要，加入 `.gitignore`。
- 加入 `.idea/`、`*.tsbuildinfo` 等本地文件忽略规则。

验收：
- `git status` 只展示真实业务改动。
- 本地 IDE 和构建缓存不干扰提交。

### 3. 迁移 Sass 废弃写法

问题：构建时 `darken()` 报废弃警告。

处理：
- 使用 Sass 模块系统中的 `color.adjust` 或 `color.scale`。
- 统一颜色处理写法。

验收：
- `npm run build` 不再出现 Sass 废弃警告。

## 推荐执行顺序

1. 修复首页按钮冒泡、编辑页浅拷贝、复制问卷深拷贝。
2. 增加填写状态校验和 store 层提交保护。
3. 固定随机题序，补齐随机选项和排序题默认答案。
4. 对齐 README：补逻辑规则、验证规则、保存进度、关闭日期。
5. 完善预览和分析页。
6. 加测试、整理 `.gitignore`、消除构建警告。

## 最近一次验证基线

- 已执行：`npm run build`
- 结果：构建成功
- 剩余警告：
  - Sass `darken()` 废弃警告
  - `AnalysisView` 相关 chunk 超过 500KB

